import { SearchField } from './search-field';

describe('SearchField', () => {
  it('should create an instance', () => {
    const directive = new SearchField();
    expect(directive).toBeTruthy();
  });
});
